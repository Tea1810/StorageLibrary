{% extends 'base.html.twig' %}

{% block title %}Carti{% endblock %}

{% block body %}
    <h1>Carti</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carti.id }}</td>
            </tr>
            <tr>
                <th>Nume_Carte</th>
                <td>{{ carti.NumeCarte }}</td>
            </tr>
            <tr>
                <th>Autor</th>
                <td>{{ carti.Autor }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ carti.Categorie }}</td>
            </tr>
            <tr>
                <th>Naționalitate</th>
                <td>{{ carti.Nationalitate }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_carti_index') }}">back to list</a>

    <a href="{{ path('app_carti_edit', {'id': carti.id}) }}">edit</a>

    {{ include('carti/_delete_form.html.twig') }}
{% endblock %}
